# CodeAlpha-Email-Extractor-
Email Extractor is a Python automation project that extracts email addresses from text files using regular expressions. It removes duplicate entries, organizes the extracted data, and saves the results in an output file with a backup copy. The project demonstrates file handling, pattern matching, and task automation in Python
